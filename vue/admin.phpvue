<?php
    $_V->composant('entete', $_JETON, [
        NOM_PAGE => 'Administration'
    ]);

    $_V->composant('utilisateurs', $_JETON, [UTILISATEUR => $_PARAMS[UTILISATEUR]]);

    $_V->composant('pied', $_JETON);
?>